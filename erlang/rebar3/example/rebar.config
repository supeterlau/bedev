{erl_opts, [debug_info]}.
{deps, []}.

{plugins, [rebar3_format]}.

{shell, [
  % {config, "config/sys.config"},
    {apps, [example]}
]}.

{format, [
    {files, ["src/*.erl", "test/*.erl"]},
    {formatter, otp_formatter},
    {options, #{paper => 80}}
]}.
