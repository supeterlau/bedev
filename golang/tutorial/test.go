package main

// https://gobyexample.com/

import (
  "fmt"
  // "strings"
)

func main() {
  /*
  // strings.SplitN
  s := strings.SplitN("a,b,c,d,e,f", ",", 0)

  s = strings.SplitN("a,b,c,d,e,f", ",", 3)

  s = strings.SplitN("a,b,c:d:e:f", ",", 5)
  fmt.Println(s)

  // fmt.Scanf : Get User Input
  var a, b int
  // fmt.Print("Enter a and b: (int)")
  fmt.Printf("Enter a and b: (int)\n")
  fmt.Scanf("%d", &a)
  fmt.Scanf("%d", &b)

  c := a + b
  fmt.Printf("%d + %d = %d\n", a, b, c)

  // exec go coroutines
  go shot()
  var next string
  fmt.Printf("Enter to contine\n")
  fmt.Scanf("%s", &next)
  fmt.Printf("Got %s\n", next)
  */
}

func shot() {
  fmt.Printf("Just shot >>>\n")
}
